Create some db manager class:

for example:
repository.AppointmentRepository, which has the database.
Call repository.AbstractRepository.load_txt to load the database according to the schema enforced (it checks the csv headers against the names of the attributes in the code)